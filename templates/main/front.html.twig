{% extends 'base.html.twig' %}

{% block title %}Liste des Offres{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="card shadow-lg border-0 rounded-4 p-4" style="background-color: white;">
            <h2 class="text-center mb-4 text-dark">📋 Liste des Offres</h2>
            
            <div class="row">
                {% for offre in offres %}
                    <div class="col-md-4 mb-4">
                        <div class="card h-100 shadow-sm border-0 rounded-3">
                            <div class="card-body">
                                <h5 class="card-title text-primary">{{ offre.title }}</h5>
                                <p class="card-text"><strong>Catégorie :</strong> {{ offre.category.name }}</p>
                                <p class="card-text">
                                    <strong>Statut :</strong> 
                                    {% if offre.status == 'archivé' %}
                                        <span class="badge bg-secondary">Archivé</span>
                                    {% else %}
                                        <span class="badge bg-success">Actif</span>
                                    {% endif %}
                                </p>
                                <a href="{{ path('app_offre_show', {'id': offre.id}) }}" class="btn btn-outline-primary btn-sm">👁️ Voir</a>
                            </div>
                        </div>
                    </div>
                {% else %}
                    <div class="col-12 text-center text-muted py-4">
                        <em>Aucune offre trouvée.</em>
                    </div>
                {% endfor %}
            </div>

            <!-- Bouton pour ajouter une nouvelle offre -->
            <div class="text-center mt-4">
                <a href="{{ path('create_offre') }}" class="btn btn-primary shadow-sm px-5 py-2">
                    ➕ Ajouter une Offre
                </a>
            </div>
        </div>
    </div>
{% endblock %}
