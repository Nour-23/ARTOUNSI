{% extends 'base.html.twig' %}

{% block title %}Catégories{% endblock %}

{% block body %}
<div class="container">
    <h1 class="text-center my-4">Liste des Catégories</h1>

  

    <!-- Liste des catégories sous forme de cartes -->
    <div class="row">
        {% for categorie in categories %}
            <div class="col-12 mb-4">
                <div class="card shadow-sm">
                    <div class="card-body">
                        <h5 class="card-title">{{ categorie.nom }}</h5>
                        <div class="d-flex justify-content-between">
                            <a href="{{ path('app_categorie_showfront', {'id': categorie.id}) }}" class="btn btn-sm btn-primary">Voir</a>
                        </div>
                    </div>
                </div>
            </div>
        {% else %}
            <div class="col-12 text-center">
                <p class="alert alert-warning">Aucune catégorie trouvée !</p>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
