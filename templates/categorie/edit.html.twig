{% extends 'base2.html.twig' %}

{% block title %}Edit Categorie{% endblock %}

{% block body %}
<div class="container">
    <h1 class="text-center my-4">Modifier la Catégorie</h1>

    <!-- Inclure le formulaire -->
    {{ include('categorie/_form.html.twig', {'button_label': 'Update'}) }}

    <!-- Bouton retour à la liste avec espacement -->
    <div class="text-center mt-4">
        <a href="{{ path('app_categorie_index') }}" class="btn btn-secondary">Retour à la liste</a>
    </div>

    <!-- Formulaire de suppression -->
    <div class="text-center mt-4">
        {{ include('categorie/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}

