{% extends 'base.html.twig' %}

{% block title %}CategoryOffre index{% endblock %}

{% block body %}
    <h1>CategoryOffre index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for category_offre in category_offres %}
            <tr>
                <td>{{ category_offre.id }}</td>
                <td>{{ category_offre.name }}</td>
                <td>{{ category_offre.libelle }}</td>
                <td>
                    <a href="{{ path('app_category_offre_show', {'id': category_offre.id}) }}">show</a>
                    <a href="{{ path('app_category_offre_edit', {'id': category_offre.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_category_offre_new') }}">Create new</a>
{% endblock %}
