{% extends 'base2.html.twig' %}

{% block title %}Modifier la Catégorie d'Offre{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="card shadow-lg border-0 rounded-4 p-4" style="background-color: white;">
            <h2 class="text-center mb-4 text-dark">📝 Modifier la Catégorie d'Offre</h2>

            <!-- Formulaire d'édition -->
            <div class="row justify-content-center">
                <div class="col-md-8">
                    {{ include('category_offre/_form.html.twig', {'button_label': 'Mettre à jour', 'class': 'btn btn-success w-100'}) }}
                </div>
            </div>

            <div class="d-flex justify-content-between mt-4">
                <a href="{{ path('app_category_offre_index') }}" class="btn btn-outline-primary shadow-sm">
                    🔙 Retour à la liste
                </a>

                <!-- Formulaire de suppression de catégorie -->
                <div>
                    {{ include('category_offre/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
