{% extends 'base.html.twig' %}

{% block title %}Créer une Nouvelle Formation{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-4 text-primary">Créer une Nouvelle Formation</h1>

        <div class="card shadow p-4">
            {{ include('formation/_form.html.twig', {'button_label': 'Créer'}) }}
        </div>

        <div class="mt-3">
            <a href="{{ path('app_formation_index') }}" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i> Retour à la liste
            </a>
        </div>
    </div>
{% endblock %}
