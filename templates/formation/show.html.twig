<<<<<<< HEAD
{% extends 'base2.html.twig' %}
=======
{% extends 'base.html.twig' %}
>>>>>>> 44358fd661ae3678fe4672afca37df15f4a237b5

{% block title %}Détails de la Formation{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-4 text-primary">Détails de la Formation</h1>

        <div class="card shadow p-4">
            <table class="table table-bordered">
                <tbody>
                    <tr>
                        <th scope="row">ID</th>
                        <td>{{ formation.id }}</td>
                    </tr>
                    <tr>
                        <th scope="row">Nom</th>
                        <td>{{ formation.nom }}</td>
                    </tr>
                    <tr>
                        <th scope="row">Description</th>
                        <td>{{ formation.description }}</td>
                    </tr>
                    <tr>
<<<<<<< HEAD
                        <th scope="row">Date de début</th>
                        <td>{{ formation.dateDebut ? formation.dateDebut|date('d/m/Y') : 'Non spécifié' }}</td>
                    </tr>
                    <tr>
                        <th scope="row">Lieu</th>
                        <td>{{ formation.lieu ?: 'Non spécifié' }}</td>
                    </tr>
                    <tr>
                        <th scope="row">Formateur</th>
                        <td>{{ formation.formateur ?: 'Non spécifié' }}</td>
                    </tr>
                    <tr>
                        <th scope="row">Tarif</th>
                        <td>{{ formation.tarif ? formation.tarif ~ ' €' : 'Gratuit' }}</td>
                    </tr>
                    <tr>
                        <th scope="row">Durée</th>
                        <td>{{ formation.duree }} heures</td>
                    </tr>
                    <tr>
                        <th scope="row">Catégorie</th>
                        <td>
                            {% if formation.category %}
                                <a href="{{ path('app_category_show', {'id': formation.category.id}) }}" class="btn btn-info btn-sm">
                                    <i class="fas fa-eye"></i> Voir la catégorie
                                </a>
                            {% else %}
                                <span class="text-muted">Aucune catégorie assignée</span>
                            {% endif %}
                        </td>
                    </tr>
=======
                        <th scope="row">Durée</th>
                        <td>{{ formation.duree }} heures</td>
                    </tr>
>>>>>>> 44358fd661ae3678fe4672afca37df15f4a237b5
                </tbody>
            </table>
        </div>

        <div class="mt-3 d-flex">
            <a href="{{ path('app_formation_index') }}" class="btn btn-secondary me-2">
                <i class="fas fa-arrow-left"></i> Retour à la liste
            </a>

            <a href="{{ path('app_formation_edit', {'id': formation.id}) }}" class="btn btn-warning me-2">
                <i class="fas fa-edit"></i> Modifier
            </a>

            {{ include('formation/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
